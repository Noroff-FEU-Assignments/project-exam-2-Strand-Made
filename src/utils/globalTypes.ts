export enum FormStatus {
  IDLE = "idle",
  SUBMITTING = "submitting",
  SUCCESS = "success",
  ERROR = "error",
}

export enum FetchStatus {
  IDLE = "idle",
  FETCHING = "fetching",
  SUCCESS = "success",
  ERROR = "error",
}
